{
  "name": "@tx-stripe/test-conformance",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@tx-stripe/protocol": "workspace:*",
    "@tx-stripe/stateless-sync": "workspace:*",
    "@tx-stripe/source-stripe": "workspace:*",
    "@tx-stripe/destination-postgres": "workspace:*",
    "@tx-stripe/destination-google-sheets": "workspace:*",
    "vitest": "^3.2.1"
  }
}
