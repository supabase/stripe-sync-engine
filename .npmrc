# @stripe packages resolve from STRIPE_NPM_REGISTRY.
# Local: Verdaccio (http://localhost:4873) — set in .envrc
# CI: GitHub Packages (https://npm.pkg.github.com)
# Unset: fails intentionally (direnv required locally, CI always sets it)
@stripe:registry=${STRIPE_NPM_REGISTRY}
