{
  "name": "@stripe/sync-api",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "vitest"
  },
  "dependencies": {
    "@hono/node-server": "^1",
    "@stripe/sync-protocol": "workspace:*",
    "hono": "^4"
  },
  "devDependencies": {
    "@types/node": "^24",
    "vitest": "^3.2"
  }
}
